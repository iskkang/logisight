# CADI (Central Asia Delay Intelligence) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CADI를 Logisight에 구축한다 — MTL이 처리한 TCR/TSR 화물의 마일스톤 딜레이를 집계·시각화·격주 보고서에 포함한다(외부비용 0, 1단계).

**Architecture:**
- Supabase 클라우드 PostgreSQL: `shipment_legs`(원시, service-role only) + `delay_index_weekly`(집계, anon read) + `disruption_events`(anon read)
- xlsx ETL: SheetJS로 `data/samples/` 파일 파싱 → Supabase upsert
- Deno Edge Functions 4개: read-only API (Cache-Control 1일)
- React 18 + Tailwind 프론트엔드: CADI 대시보드 + Home + News + Methodology
- 기존 뉴스레터 파이프라인 확장: 격주 보고서에 CADI 섹션 삽입

**Tech Stack:** React 18 + TypeScript + Vite + Tailwind + Recharts + @supabase/supabase-js + SheetJS(xlsx) + @anthropic-ai/sdk + date-fns + react-i18next + react-router-dom

**확정 사항:**
- Supabase: 클라우드 (B)
- SheetJS(xlsx): ✅ 승인
- 발행 주기: 격주
- 스펙 파일(CADI_Spec_v1.1 등): 추후 추가 시 Phase 2 컬럼매핑·Phase 3 템플릿 업데이트

---

## ⚠️ 사전 작업 (코딩 전 사용자가 완료해야 함)

```
1. supabase.com → 새 프로젝트 생성 (무료 플랜)
   → 프로젝트 URL: https://<ref>.supabase.co
   → Settings > API 에서 anon key + service_role key 복사

2. .env.local 생성 (gitignore 확인 — 절대 commit 금지):
   VITE_SUPABASE_URL=https://<ref>.supabase.co
   VITE_SUPABASE_ANON_KEY=<anon_key>
   SUPABASE_URL=https://<ref>.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=<service_role_key>
   (기존 ANTHROPIC_API_KEY, RESEND_API_KEY 유지)

3. 프로젝트 ref 메모 (supabase link 시 필요):
   URL 예: https://abcdefgh.supabase.co → ref = "abcdefgh"

4. data/samples/ 폴더에 xlsx 파일 5개 추가 (Phase 2용)
   ※ 없어도 Phase 0.5~1 진행 가능, Phase 2 실행 전 추가 필요
```

---

## File Structure (전체 생성/수정 파일)

| 파일 | 상태 | Phase |
|------|------|-------|
| `.env.example` | Modify | 0.5 |
| `src/lib/supabase.ts` | Create ⚠️ | 0.5 |
| `src/main.tsx` | Create | 0.5 |
| `index.html` | Create | 0.5 |
| `supabase/migrations/20260523000001_cadi_schema.sql` | Create | 1 |
| `package.json` | Modify (xlsx + script) | 2 |
| `workers/collectors/utils/xlsx_parser.ts` | Create | 2 |
| `workers/collectors/tracing_ingest.ts` | Create | 2 |
| `scripts/generate-cadi-report.js` | Create | 3 |
| `content/templates/cadi-weekly-template.md` | Create | 3 |
| `supabase/functions/cadi-lanes/index.ts` | Create | 4 |
| `supabase/functions/cadi-weekly/index.ts` | Create | 4 |
| `supabase/functions/alerts/index.ts` | Create | 4 |
| `supabase/functions/reports/index.ts` | Create | 4 |
| `src/App.tsx` | Create | 5 |
| `src/lib/api.ts` | Create | 5 |
| `src/pages/Home.tsx` | Create | 5 |
| `src/pages/CadiDashboard.tsx` | Create | 5 |
| `src/pages/NewsAnalysis.tsx` | Create | 5 |
| `src/pages/Methodology.tsx` | Create | 5 |
| `src/components/cadi/DelayChart.tsx` | Create | 5 |
| `src/components/cadi/AlertCard.tsx` | Create | 5 |
| `src/components/cadi/DataQualityBadge.tsx` | Create | 5 |
| `src/components/cadi/LaneSelector.tsx` | Create | 5 |
| `src/components/shared/LoadingState.tsx` | Create | 5 |
| `src/components/shared/EmptyState.tsx` | Create | 5 |
| `src/locales/en.json` | Create | 5 |
| `src/locales/ko.json` | Create | 5 |
| `src/locales/ru.json` | Create | 5 |
| `src/locales/uz.json` | Create | 5 |
| `src/locales/zh.json` | Create | 5 |
| `.github/workflows/cadi-ingest.yml` | Create | 6 |
| `.github/workflows/weekly-report.yml` | Modify (CADI 섹션 추가) | 6 |

---

## Task 0.5: Supabase Bootstrap + Vite 엔트리

**Files:**
- Modify: `.env.example`
- Create: `src/lib/supabase.ts` ⚠️ (CLAUDE.md 3.3 — 사용자 확인 영역, 생성 시 알릴 것)
- Create: `src/main.tsx`
- Create: `index.html`

- [ ] **Step 1: .env.example에 Supabase + CADI 변수 추가**

```
# .env.example 끝에 추가 (기존 내용 보존)

# Supabase (frontend — VITE_ prefix 필수)
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase (backend/workers — VITE_ prefix 없음)
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

- [ ] **Step 2: ⚠️ 사용자에게 알리고 `src/lib/supabase.ts` 생성**

> "CLAUDE.md 3.3에 따라 src/lib/supabase.ts는 사용자 확인 영역입니다. 생성합니다."

```typescript
// src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (!supabaseUrl || !supabaseAnonKey) {
  throw new Error(
    'Missing VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY in .env.local'
  );
}

export const supabase = createClient(supabaseUrl, supabaseAnonKey);
```

- [ ] **Step 3: `index.html` 생성 (Vite 엔트리)**

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Logisight — Central Asia Delay Intelligence</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 4: `src/main.tsx` 생성**

```typescript
// src/main.tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

- [ ] **Step 5: `src/index.css` 생성 (Tailwind 진입점)**

```css
/* src/index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 6: Supabase CLI로 클라우드 프로젝트 링크**

```bash
# 사용자가 .env.local의 SUPABASE_URL에서 ref를 확인 후 실행
# URL = https://abcdefgh.supabase.co → ref = abcdefgh
supabase link --project-ref <ref>
```

Expected: `Finished supabase link.`

만약 `supabase` 명령이 없으면: `npm install -g supabase`

- [ ] **Step 7: 연결 확인**

```bash
supabase status
```

Expected: 프로젝트 정보 + DB URL 표시

- [ ] **Step 8: Commit**

```bash
git add .env.example src/lib/supabase.ts src/main.tsx src/index.css index.html
git commit -m "feat(bootstrap): Supabase cloud link + Vite entry points"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 1: DB 스키마 마이그레이션

**Files:**
- Create: `supabase/migrations/20260523000001_cadi_schema.sql`

- [ ] **Step 1: 마이그레이션 파일 작성**

```sql
-- supabase/migrations/20260523000001_cadi_schema.sql
-- CADI: Central Asia Delay Intelligence schema
-- Milestones: ORIGIN_DEP, SEA_TS_ARR, RAIL_DEP_CN, KASHI_ARR, KASHI_BONDED,
--             TRUCK_DEP, XIAN_HUB, CN_BORDER, KG_UZ_BORDER, DEST_ARR

-- ── Enums ────────────────────────────────────────────────────────────────────
CREATE TYPE milestone_code AS ENUM (
  'ORIGIN_DEP',
  'SEA_TS_ARR',
  'RAIL_DEP_CN',
  'KASHI_ARR',
  'KASHI_BONDED',
  'TRUCK_DEP',
  'XIAN_HUB',
  'CN_BORDER',
  'KG_UZ_BORDER',
  'DEST_ARR'
);

CREATE TYPE data_quality_level AS ENUM ('confirmed', 'provisional', 'indicative');

CREATE TYPE disruption_severity AS ENUM ('high', 'medium', 'low');

-- ── lanes ─────────────────────────────────────────────────────────────────────
CREATE TABLE lanes (
  id           TEXT PRIMARY KEY,   -- 'TCR' | 'TSR' | 'TITR' | 'TMR' | 'TMGR'
  name_en      TEXT NOT NULL,
  name_zh      TEXT,
  name_ru      TEXT,
  name_ko      TEXT,
  transit_min  INTEGER,            -- planned transit days (min)
  transit_max  INTEGER,            -- planned transit days (max)
  border_points TEXT[],            -- key border crossings
  created_at   TIMESTAMPTZ DEFAULT NOW()
);

-- ── shipment_legs (service-role only — RLS blocks anon) ──────────────────────
CREATE TABLE shipment_legs (
  id               UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  lane_id          TEXT NOT NULL REFERENCES lanes(id),
  shipment_ref     TEXT NOT NULL,     -- anonymized reference (ETL strips real B/L)
  week_iso         TEXT NOT NULL,     -- 'YYYY-WNN' e.g. '2026-W20'
  milestone        milestone_code NOT NULL,
  planned_at       TIMESTAMPTZ,
  actual_at        TIMESTAMPTZ,
  delay_hours      NUMERIC,           -- actual_at - planned_at in hours (negative = early)
  flag             TEXT,              -- e.g. 'date_parse_error', 'actual_missing'
  raw_source_file  TEXT,              -- source xlsx filename (no path)
  created_at       TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (shipment_ref, milestone)
);

-- ── delay_index_weekly (anon read) ───────────────────────────────────────────
CREATE TABLE delay_index_weekly (
  id              UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  lane_id         TEXT NOT NULL REFERENCES lanes(id),
  week_iso        TEXT NOT NULL,
  milestone       milestone_code NOT NULL,
  median_delay_h  NUMERIC,            -- median delay in hours
  p90_delay_h     NUMERIC,            -- 90th percentile delay in hours
  on_time_rate    NUMERIC,            -- fraction with delay_hours <= 0
  sample_count    INTEGER NOT NULL,
  data_quality    data_quality_level NOT NULL,
  gap_vs_premium  NUMERIC,            -- gap vs premium lane median (hours, optional)
  updated_at      TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE (lane_id, week_iso, milestone)
);

-- ── disruption_events (anon read) ────────────────────────────────────────────
CREATE TABLE disruption_events (
  id             UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  lane_id        TEXT REFERENCES lanes(id),   -- NULL = affects all lanes
  event_type     TEXT NOT NULL,               -- 'BORDER_CLOSURE' | 'CUSTOMS_DELAY' | 'POLICY_CHANGE' | 'CAPACITY'
  title_en       TEXT NOT NULL,
  title_ru       TEXT,
  title_zh       TEXT,
  title_ko       TEXT,
  description_en TEXT,
  description_ru TEXT,
  started_at     TIMESTAMPTZ,
  resolved_at    TIMESTAMPTZ,                 -- NULL = ongoing
  severity       disruption_severity NOT NULL DEFAULT 'medium',
  source_url     TEXT,
  created_at     TIMESTAMPTZ DEFAULT NOW()
);

-- ── RLS Policies ─────────────────────────────────────────────────────────────
ALTER TABLE lanes ENABLE ROW LEVEL SECURITY;
ALTER TABLE shipment_legs ENABLE ROW LEVEL SECURITY;
ALTER TABLE delay_index_weekly ENABLE ROW LEVEL SECURITY;
ALTER TABLE disruption_events ENABLE ROW LEVEL SECURITY;

-- lanes: anyone can read
CREATE POLICY "lanes_public_read" ON lanes
  FOR SELECT TO anon, authenticated USING (true);

-- shipment_legs: service_role only (anon blocked — raw data)
-- No anon policy → anon gets 0 rows

-- delay_index_weekly: anyone can read aggregated data
CREATE POLICY "delay_index_public_read" ON delay_index_weekly
  FOR SELECT TO anon, authenticated USING (true);

-- disruption_events: anyone can read
CREATE POLICY "disruption_events_public_read" ON disruption_events
  FOR SELECT TO anon, authenticated USING (true);

-- ── Seed: Lane Definitions ────────────────────────────────────────────────────
INSERT INTO lanes (id, name_en, name_zh, name_ru, name_ko, transit_min, transit_max, border_points) VALUES
  ('TCR',  'Trans-China Railway',        '中欧班列',          'Транскитайская ж/д',  'TCR 중국 횡단', 23, 30, ARRAY['Erenhot', 'Khorgos', 'Dostyk']),
  ('TSR',  'Trans-Siberian Railway',     '西伯利亚大铁路',     'Транссибирская ж/д',  'TSR 시베리아', 20, 27, ARRAY['Zabaikalsk', 'Vladivostok']),
  ('TITR', 'Trans-Caspian (Middle Corridor)', '中间走廊',    'Средний коридор',     'TITR 중간 회랑', 38, 45, ARRAY['Aktau', 'Baku', 'Poti']),
  ('TMR',  'Trans-Manchurian Railway',   '满洲里铁路',         'Трансманьчжурская ж/д', 'TMR 만주', 25, 35, ARRAY['Manzhouli', 'Zabaykalsk']),
  ('TMGR', 'Trans-Mongolian Railway',    '蒙古铁路',           'Трансмонгольская ж/д',  'TMGR 몽골', 28, 38, ARRAY['Erlian', 'Ulaanbaatar'])
ON CONFLICT (id) DO NOTHING;
```

- [ ] **Step 2: 마이그레이션 적용**

```bash
supabase db push
```

Expected: `Finished supabase db push.`

실패 시: `supabase migration list` 로 상태 확인 후 오류 메시지 분석.

- [ ] **Step 3: RLS 검증 — anon이 shipment_legs 조회 불가 확인**

Supabase Dashboard → SQL Editor:

```sql
-- 이 쿼리는 0건을 반환해야 함 (anon RLS 차단)
SET ROLE anon;
SELECT count(*) FROM shipment_legs;
-- Expected: 0
RESET ROLE;

-- 이 쿼리는 5건을 반환해야 함 (lanes 시드 확인)
SELECT id, name_en, transit_min, transit_max FROM lanes;
-- Expected: TCR, TSR, TITR, TMR, TMGR
```

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260523000001_cadi_schema.sql
git commit -m "feat(db): CADI schema — lanes/shipment_legs/delay_index_weekly/disruption_events + RLS"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 2: ETL — xlsx 트레이싱 인제스트

**Files:**
- Modify: `package.json` (xlsx 의존성 + collect:tracing 스크립트)
- Create: `workers/collectors/utils/xlsx_parser.ts`
- Create: `workers/collectors/tracing_ingest.ts`

> ⚠️ 스펙 파일(CADI_Spec_v1.1 5·6절)이 없으므로 컬럼 매핑은 합리적 가정으로 작성.
> 실제 xlsx 수령 후 `xlsx_parser.ts`의 `COLUMN_MAP` 배열을 업데이트할 것.

- [ ] **Step 1: SheetJS 설치 (승인됨)**

```bash
npm install xlsx
```

Expected: package-lock.json 갱신, node_modules/xlsx 생성

- [ ] **Step 2: `package.json` scripts에 `collect:tracing` 추가**

`package.json`의 `"scripts"` 블록:
```json
"collect:tracing": "ts-node --project tsconfig.workers.json workers/collectors/tracing_ingest.ts"
```

- [ ] **Step 3: `workers/collectors/utils/xlsx_parser.ts` 작성**

```typescript
// workers/collectors/utils/xlsx_parser.ts
// Maps xlsx column headers (EN/中/RU) to CADI milestone codes.
// Update COLUMN_MAP when CADI_Spec_v1.1 is available.

import * as XLSX from 'xlsx';

export type MilestoneCode =
  | 'ORIGIN_DEP'
  | 'SEA_TS_ARR'
  | 'RAIL_DEP_CN'
  | 'KASHI_ARR'
  | 'KASHI_BONDED'
  | 'TRUCK_DEP'
  | 'XIAN_HUB'
  | 'CN_BORDER'
  | 'KG_UZ_BORDER'
  | 'DEST_ARR';

export type DateVariant = 'planned' | 'actual';

export interface MilestoneCell {
  milestone: MilestoneCode;
  variant: DateVariant;
  colIndex: number;
}

// Keywords that map to each milestone (case-insensitive substring match)
// Add aliases from CADI_Spec_v1.1 when available
const MILESTONE_KEYWORDS: Array<{
  milestone: MilestoneCode;
  variant: DateVariant;
  keywords: string[];
}> = [
  { milestone: 'ORIGIN_DEP',  variant: 'planned', keywords: ['etd', 'origin dep', 'pol etd', '起运', 'отправление план'] },
  { milestone: 'ORIGIN_DEP',  variant: 'actual',  keywords: ['atd', 'origin actual', 'actual dep', '实际起运', 'отправление факт'] },
  { milestone: 'SEA_TS_ARR',  variant: 'planned', keywords: ['sea arr plan', 'transit port plan', 'sea eta', '中转港计划', 'мор прибытие план'] },
  { milestone: 'SEA_TS_ARR',  variant: 'actual',  keywords: ['sea arr act', 'transit port act', 'sea ata', '中转港实际', 'мор прибытие факт'] },
  { milestone: 'RAIL_DEP_CN', variant: 'planned', keywords: ['rail dep plan', 'cre dep plan', '铁路出发计划', 'жд отправление план'] },
  { milestone: 'RAIL_DEP_CN', variant: 'actual',  keywords: ['rail dep act', 'cre dep act', '铁路出发实际', 'жд отправление факт'] },
  { milestone: 'KASHI_ARR',   variant: 'planned', keywords: ['kashi plan', 'kashgar plan', 'urumqi plan', '喀什计划', '乌鲁木齐计划', 'каши план'] },
  { milestone: 'KASHI_ARR',   variant: 'actual',  keywords: ['kashi act', 'kashgar act', 'urumqi act', '喀什实际', 'каши факт'] },
  { milestone: 'KASHI_BONDED',variant: 'planned', keywords: ['bonded plan', 'kashi bond plan', '监管仓计划', 'таможня план'] },
  { milestone: 'KASHI_BONDED',variant: 'actual',  keywords: ['bonded act', 'kashi bond act', '监管仓实际', 'таможня факт'] },
  { milestone: 'TRUCK_DEP',   variant: 'planned', keywords: ['truck dep plan', 'road dep plan', '卡车出发计划', 'авто отправление план'] },
  { milestone: 'TRUCK_DEP',   variant: 'actual',  keywords: ['truck dep act', 'road dep act', '卡车出发实际', 'авто отправление факт'] },
  { milestone: 'XIAN_HUB',    variant: 'planned', keywords: ["xi'an plan", 'xian plan', '西安计划', 'сиань план'] },
  { milestone: 'XIAN_HUB',    variant: 'actual',  keywords: ["xi'an act", 'xian act', '西安实际', 'сиань факт'] },
  { milestone: 'CN_BORDER',   variant: 'planned', keywords: ['cn border plan', 'china border plan', '中国出境计划', 'граница кнр план'] },
  { milestone: 'CN_BORDER',   variant: 'actual',  keywords: ['cn border act', 'china border act', '中国出境实际', 'граница кнр факт'] },
  { milestone: 'KG_UZ_BORDER',variant: 'planned', keywords: ['kg border plan', 'uz border plan', 'kz border plan', '中亚边境计划', 'граница ца план'] },
  { milestone: 'KG_UZ_BORDER',variant: 'actual',  keywords: ['kg border act', 'uz border act', 'kz border act', '中亚边境实际', 'граница ца факт'] },
  { milestone: 'DEST_ARR',    variant: 'planned', keywords: ['eta', 'dest eta', 'destination plan', '目的地计划', 'прибытие план'] },
  { milestone: 'DEST_ARR',    variant: 'actual',  keywords: ['ata', 'dest act', 'destination act', '目的地实际', 'прибытие факт'] },
];

/** Detect route/lane from file path or sheet content */
export function detectLane(filePath: string, cellValues: string[]): string {
  const combined = [filePath, ...cellValues].join(' ').toLowerCase();
  if (combined.includes('titr') || combined.includes('middle corridor') || combined.includes('caspian')) return 'TITR';
  if (combined.includes('tsr') || combined.includes('siberia') || combined.includes('владивосток')) return 'TSR';
  if (combined.includes('mongol') || combined.includes('tmgr')) return 'TMGR';
  if (combined.includes('manchur') || combined.includes('tmr')) return 'TMR';
  return 'TCR'; // default
}

/** Try to find header row (checks rows 0, 2, 4 for milestone keywords) */
function findHeaderRow(sheet: XLSX.WorkSheet): number {
  const range = XLSX.utils.decode_range(sheet['!ref'] ?? 'A1:A1');
  for (const rowIdx of [0, 1, 2, 3, 4]) {
    if (rowIdx > range.e.r) break;
    let matchCount = 0;
    for (let c = range.s.c; c <= range.e.c; c++) {
      const cell = sheet[XLSX.utils.encode_cell({ r: rowIdx, c })];
      if (!cell) continue;
      const v = String(cell.v ?? '').toLowerCase();
      if (MILESTONE_KEYWORDS.some(m => m.keywords.some(kw => v.includes(kw)))) matchCount++;
    }
    if (matchCount >= 2) return rowIdx;
  }
  return 0; // fallback
}

/** Map header row to milestone cells */
function mapHeaders(sheet: XLSX.WorkSheet, headerRow: number): MilestoneCell[] {
  const range = XLSX.utils.decode_range(sheet['!ref'] ?? 'A1:A1');
  const cells: MilestoneCell[] = [];
  for (let c = range.s.c; c <= range.e.c; c++) {
    const cell = sheet[XLSX.utils.encode_cell({ r: headerRow, c })];
    if (!cell) continue;
    const v = String(cell.v ?? '').toLowerCase();
    for (const m of MILESTONE_KEYWORDS) {
      if (m.keywords.some(kw => v.includes(kw))) {
        cells.push({ milestone: m.milestone, variant: m.variant, colIndex: c });
        break;
      }
    }
  }
  return cells;
}

/** Normalize a cell value to ISO date string or null */
export function normalizeDate(cell: XLSX.CellObject | undefined): {
  iso: string | null;
  flag: string | null;
} {
  if (!cell) return { iso: null, flag: null };
  const v = cell.v;

  // Excel serial number (numeric date)
  if (cell.t === 'n' && typeof v === 'number') {
    const d = XLSX.SSF.parse_date_code(v);
    if (d) {
      const dt = new Date(Date.UTC(d.y, d.m - 1, d.d, d.H ?? 0, d.M ?? 0, d.S ?? 0));
      return { iso: dt.toISOString(), flag: null };
    }
    return { iso: null, flag: 'invalid_excel_serial' };
  }

  // String date
  if (typeof v === 'string') {
    const s = v.trim();
    if (!s) return { iso: null, flag: null };

    // Common formats: YYYY-MM-DD, DD/MM/YYYY, MM/DD/YYYY, YYYY.MM.DD
    const patterns = [
      /^(\d{4})-(\d{1,2})-(\d{1,2})/, // ISO
      /^(\d{1,2})\/(\d{1,2})\/(\d{4})/, // MM/DD/YYYY
      /^(\d{4})\.(\d{1,2})\.(\d{1,2})/, // YYYY.MM.DD
    ];
    for (const p of patterns) {
      const m = s.match(p);
      if (m) {
        const d = new Date(s);
        if (!isNaN(d.getTime())) return { iso: d.toISOString(), flag: null };
      }
    }

    // Last resort: Date constructor
    const d = new Date(s);
    if (!isNaN(d.getTime())) return { iso: d.toISOString(), flag: 'date_string_parsed' };
    return { iso: null, flag: `date_parse_failed:${s}` };
  }

  // Date object (rare in xlsx)
  if (v instanceof Date) {
    return { iso: v.toISOString(), flag: null };
  }

  return { iso: null, flag: `unknown_cell_type:${cell.t}` };
}

export interface ParsedShipment {
  shipmentRef: string;
  weekIso: string;
  laneId: string;
  milestones: Array<{
    milestone: MilestoneCode;
    plannedAt: string | null;
    actualAt: string | null;
    delayHours: number | null;
    flag: string | null;
  }>;
}

/** Parse one xlsx file → array of ParsedShipment */
export function parseXlsx(filePath: string): ParsedShipment[] {
  const workbook = XLSX.readFile(filePath);
  const results: ParsedShipment[] = [];

  for (const sheetName of workbook.SheetNames) {
    const sheet = workbook.Sheets[sheetName];
    if (!sheet) continue;

    const range = XLSX.utils.decode_range(sheet['!ref'] ?? 'A1:A1');
    const headerRow = findHeaderRow(sheet);
    const mappedCols = mapHeaders(sheet, headerRow);
    if (mappedCols.length === 0) {
      console.warn(`⚠️ [${filePath}] sheet "${sheetName}": no milestone columns found`);
      continue;
    }

    // Detect lane from first 20 cell values
    const sampleValues: string[] = [];
    for (let r = headerRow; r <= Math.min(headerRow + 5, range.e.r); r++) {
      for (let c = range.s.c; c <= Math.min(range.s.c + 10, range.e.c); c++) {
        const cell = sheet[XLSX.utils.encode_cell({ r, c })];
        if (cell) sampleValues.push(String(cell.v ?? ''));
      }
    }
    const laneId = detectLane(filePath, sampleValues);

    // Assume first non-header column is shipment ref
    const refColIdx = range.s.c;

    // Group planned/actual by milestone
    const milestoneMap = new Map<MilestoneCode, { plannedIdx: number | null; actualIdx: number | null }>();
    for (const mc of mappedCols) {
      if (!milestoneMap.has(mc.milestone)) {
        milestoneMap.set(mc.milestone, { plannedIdx: null, actualIdx: null });
      }
      const entry = milestoneMap.get(mc.milestone)!;
      if (mc.variant === 'planned') entry.plannedIdx = mc.colIndex;
      else entry.actualIdx = mc.colIndex;
    }

    // Parse data rows
    for (let r = headerRow + 1; r <= range.e.r; r++) {
      const refCell = sheet[XLSX.utils.encode_cell({ r, c: refColIdx })];
      const shipmentRef = refCell ? String(refCell.v ?? '').trim() : '';
      if (!shipmentRef) continue; // skip empty rows

      // Determine week from ORIGIN_DEP actual or planned
      let weekIso = '2026-W00';
      const originEntry = milestoneMap.get('ORIGIN_DEP');
      if (originEntry) {
        const origPlanned = normalizeDate(sheet[XLSX.utils.encode_cell({ r, c: originEntry.plannedIdx ?? -1 })]);
        const origActual  = normalizeDate(sheet[XLSX.utils.encode_cell({ r, c: originEntry.actualIdx ?? -1 })]);
        const isoStr = origActual.iso ?? origPlanned.iso;
        if (isoStr) {
          const d = new Date(isoStr);
          const jan4 = new Date(d.getFullYear(), 0, 4);
          const weekNum = Math.ceil((((d.getTime() - jan4.getTime()) / 86400000) + jan4.getDay() + 1) / 7);
          weekIso = `${d.getFullYear()}-W${String(weekNum).padStart(2, '0')}`;
        }
      }

      const shipment: ParsedShipment = {
        shipmentRef: `${filePath.replace(/.*[\\/]/, '')}:row${r}:${shipmentRef}`,
        weekIso,
        laneId,
        milestones: [],
      };

      for (const [milestone, { plannedIdx, actualIdx }] of milestoneMap) {
        const planned = normalizeDate(
          plannedIdx !== null ? sheet[XLSX.utils.encode_cell({ r, c: plannedIdx })] : undefined
        );
        const actual = normalizeDate(
          actualIdx !== null ? sheet[XLSX.utils.encode_cell({ r, c: actualIdx })] : undefined
        );

        let delayHours: number | null = null;
        if (planned.iso && actual.iso) {
          delayHours = (new Date(actual.iso).getTime() - new Date(planned.iso).getTime()) / 3_600_000;
        }

        const flag = planned.flag ?? actual.flag ?? null;

        shipment.milestones.push({ milestone, plannedAt: planned.iso, actualAt: actual.iso, delayHours, flag });
      }

      results.push(shipment);
    }
  }

  return results;
}

/** Aggregate ParsedShipment[] → weekly delay metrics per milestone */
export function aggregateWeekly(
  shipments: ParsedShipment[]
): Map<string, { laneId: string; weekIso: string; milestone: MilestoneCode; delayHours: number[] }> {
  const buckets = new Map<string, { laneId: string; weekIso: string; milestone: MilestoneCode; delayHours: number[] }>();

  for (const s of shipments) {
    for (const m of s.milestones) {
      if (m.delayHours === null) continue;
      const key = `${s.laneId}|${s.weekIso}|${m.milestone}`;
      if (!buckets.has(key)) {
        buckets.set(key, { laneId: s.laneId, weekIso: s.weekIso, milestone: m.milestone, delayHours: [] });
      }
      buckets.get(key)!.delayHours.push(m.delayHours);
    }
  }

  return buckets;
}

/** Compute median of a number array */
export function median(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  const mid = Math.floor(sorted.length / 2);
  return sorted.length % 2 !== 0 ? sorted[mid] : (sorted[mid - 1] + sorted[mid]) / 2;
}

/** Compute p90 of a number array */
export function p90(values: number[]): number {
  const sorted = [...values].sort((a, b) => a - b);
  const idx = Math.ceil(0.9 * sorted.length) - 1;
  return sorted[Math.max(0, idx)];
}

/** Classify data quality */
export function dataQuality(n: number): 'confirmed' | 'provisional' | 'indicative' {
  if (n >= 5) return 'confirmed';
  if (n >= 2) return 'provisional';
  return 'indicative';
}
```

- [ ] **Step 4: `workers/collectors/tracing_ingest.ts` 작성**

```typescript
// workers/collectors/tracing_ingest.ts
// Reads xlsx files from data/samples/, parses milestones, upserts to Supabase.
// Run: npm run collect:tracing
// Idempotent: UNIQUE constraint on (shipment_ref, milestone) and (lane_id, week_iso, milestone).

import * as fs from 'fs';
import * as path from 'path';
import { createClient } from '@supabase/supabase-js';
import {
  parseXlsx,
  aggregateWeekly,
  median,
  p90,
  dataQuality,
  type MilestoneCode,
} from './utils/xlsx_parser';

const SAMPLES_DIR = path.resolve(process.cwd(), 'data', 'samples');

function getSupabase() {
  const url = process.env.SUPABASE_URL;
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!url || !key) {
    throw new Error('Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY');
  }
  return createClient(url, key);
}

async function main() {
  if (!fs.existsSync(SAMPLES_DIR)) {
    console.error(`❌ data/samples/ 디렉터리가 없습니다. 생성 후 xlsx 파일을 넣어주세요.`);
    process.exit(1);
  }

  const xlsxFiles = fs.readdirSync(SAMPLES_DIR).filter(f => f.endsWith('.xlsx') || f.endsWith('.xls'));
  if (xlsxFiles.length === 0) {
    console.warn('⚠️ data/samples/에 xlsx 파일이 없습니다. 스킵.');
    return;
  }

  console.log(`📂 ${xlsxFiles.length}개 파일 발견: ${xlsxFiles.join(', ')}`);

  const supabase = getSupabase();
  let totalShipments = 0;
  let totalFlags = 0;

  // ── 1. Parse all xlsx files ─────────────────────────────────────────────────
  const allShipments = xlsxFiles.flatMap(file => {
    const filePath = path.join(SAMPLES_DIR, file);
    console.log(`\n📄 파싱 중: ${file}`);
    try {
      const shipments = parseXlsx(filePath);
      console.log(`  → ${shipments.length}건 파싱 완료`);
      totalShipments += shipments.length;

      // Log flags
      for (const s of shipments) {
        for (const m of s.milestones) {
          if (m.flag) {
            console.warn(`  ⚠️ 플래그 [${s.shipmentRef}] ${m.milestone}: ${m.flag}`);
            totalFlags++;
          }
        }
      }

      return shipments;
    } catch (err) {
      console.error(`  ❌ 파싱 실패: ${(err as Error).message}`);
      return [];
    }
  });

  if (allShipments.length === 0) {
    console.warn('⚠️ 유효한 화물 데이터가 없습니다.');
    return;
  }

  // ── 2. Upsert shipment_legs (raw, service-role only) ────────────────────────
  const legRows = allShipments.flatMap(s =>
    s.milestones.map(m => ({
      lane_id: s.laneId,
      shipment_ref: s.shipmentRef,
      week_iso: s.weekIso,
      milestone: m.milestone,
      planned_at: m.plannedAt,
      actual_at: m.actualAt,
      delay_hours: m.delayHours,
      flag: m.flag,
      raw_source_file: s.shipmentRef.split(':')[0], // filename part
    }))
  );

  console.log(`\n💾 shipment_legs upsert: ${legRows.length}건`);
  const { error: legError } = await supabase
    .from('shipment_legs')
    .upsert(legRows, { onConflict: 'shipment_ref,milestone', ignoreDuplicates: false });

  if (legError) {
    console.error('❌ shipment_legs upsert 실패:', legError.message);
    process.exit(1);
  }
  console.log('✅ shipment_legs upsert 완료');

  // ── 3. Aggregate → delay_index_weekly upsert ────────────────────────────────
  const buckets = aggregateWeekly(allShipments);
  const indexRows: Array<{
    lane_id: string;
    week_iso: string;
    milestone: MilestoneCode;
    median_delay_h: number;
    p90_delay_h: number;
    on_time_rate: number;
    sample_count: number;
    data_quality: string;
    updated_at: string;
  }> = [];

  for (const [, bucket] of buckets) {
    const n = bucket.delayHours.length;
    const med = median(bucket.delayHours);
    const p = p90(bucket.delayHours);
    const onTime = bucket.delayHours.filter(h => h <= 0).length / n;
    const quality = dataQuality(n);

    indexRows.push({
      lane_id: bucket.laneId,
      week_iso: bucket.weekIso,
      milestone: bucket.milestone,
      median_delay_h: Math.round(med * 10) / 10,
      p90_delay_h: Math.round(p * 10) / 10,
      on_time_rate: Math.round(onTime * 1000) / 1000,
      sample_count: n,
      data_quality: quality,
      updated_at: new Date().toISOString(),
    });
  }

  console.log(`\n📊 delay_index_weekly upsert: ${indexRows.length}건`);
  const { error: idxError } = await supabase
    .from('delay_index_weekly')
    .upsert(indexRows, { onConflict: 'lane_id,week_iso,milestone', ignoreDuplicates: false });

  if (idxError) {
    console.error('❌ delay_index_weekly upsert 실패:', idxError.message);
    process.exit(1);
  }
  console.log('✅ delay_index_weekly upsert 완료');

  // ── 4. Summary ────────────────────────────────────────────────────────────────
  console.log(`
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 CADI 트레이싱 인제스트 완료
  파일      : ${xlsxFiles.length}개
  화물 건수 : ${totalShipments}건
  플래그    : ${totalFlags}건 (로그 확인)
  집계 레코드: ${indexRows.length}건
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`);
}

main().catch(err => {
  console.error('❌ tracing_ingest 실패:', err);
  process.exit(1);
});
```

- [ ] **Step 5: data/samples/ 디렉터리 생성 + .gitkeep**

```bash
mkdir -p data/samples
touch data/samples/.gitkeep
```

- [ ] **Step 6: .gitignore에 xlsx 파일 제외 확인 (개인정보 보호)**

`.gitignore`에 다음 추가:
```
data/samples/*.xlsx
data/samples/*.xls
```

- [ ] **Step 7: xlsx 파일 없이 dry-run 검증**

```bash
npm run collect:tracing
```

Expected:
```
❌ data/samples/ 디렉터리가 없습니다...
```
또는 (디렉터리 있고 파일 없으면):
```
⚠️ data/samples/에 xlsx 파일이 없습니다. 스킵.
```

(실제 xlsx 추가 후 전체 검증은 Phase 2 실행 마지막 단계)

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json workers/collectors/utils/xlsx_parser.ts workers/collectors/tracing_ingest.ts data/samples/.gitkeep .gitignore
git commit -m "feat(etl): CADI xlsx tracing ingest — SheetJS parser + Supabase upsert"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 3: CADI 격주 보고서 생성기 (기존 뉴스레터 파이프라인 확장)

**Files:**
- Create: `content/templates/cadi-weekly-template.md`
- Create: `scripts/generate-cadi-report.js`

> ⚠️ CentralAsia_Weekly_Template.md 없음 → PRD v1.1 + 기존 MTL Vol.02 패턴 기반 작성.
> 실제 템플릿 수령 후 SECTION_RULES와 TEMPLATE 문자열 업데이트.

- [ ] **Step 1: 템플릿 파일 작성**

```markdown
<!-- content/templates/cadi-weekly-template.md -->
<!-- CADI 격주 주보 템플릿 — 빈 답변 섹션은 자동 생략, 날조 금지 -->
<!-- 실제 CentralAsia_Weekly_Template 수령 시 업데이트 -->

# 중앙아시아 물류 딜레이 인텔리전스 (CADI)
**발행**: MTL Shipping Agency | **주기**: 격주
**신뢰 표기**: MTL 처리 화물 기준 집계 (n=샘플수)

---

## 1. 이번 주 딜레이 요약

<!-- delay_index_weekly 데이터로 채움 (data_quality 표기 필수) -->
<!-- 데이터 없으면 이 섹션 통째로 생략 -->

| 노선 | 마일스톤 | 중앙값 딜레이 | P90 딜레이 | 정시율 | 신뢰도 (n) |
|------|----------|-----------|---------|------|-----------|
{{DELAY_TABLE}}

---

## 2. 국경/정책 이슈 (Disruption Alert)

<!-- disruption_events 테이블 + 16문항 중 ②번 답변 -->
<!-- 데이터 없으면 "이번 주 특이사항 없음" 기재 -->

{{DISRUPTION_SECTION}}

---

## 3. 현상→원인→전망

<!-- research-market-analyst 분석 결과 삽입 -->
<!-- 질문지 답변이 없으면 이 섹션 생략 -->

{{ANALYSIS_SECTION}}

---

## 4. MTL 시사점

<!-- 이 섹션은 영업팀이 수동으로 작성 (자동 생성 금지) -->

_MTL 영업팀 검토 후 작성_

---

**데이터 출처**: MTL 처리 화물 (익명 집계), 발행일: {{PUBLISH_DATE}}
**면책**: 본 보고서는 MTL 화물 샘플 기반이며 시장 전체를 대표하지 않습니다.
```

- [ ] **Step 2: `scripts/generate-cadi-report.js` 작성**

```javascript
// scripts/generate-cadi-report.js
// 기존 weekly-report.yml에서 호출됨.
// delay_index_weekly + disruption_events → CADI 보고서 초안 → content/drafts/

'use strict';
const { createClient } = require('@supabase/supabase-js');
const fs = require('fs');
const path = require('path');

const SUPABASE_URL = process.env.SUPABASE_URL;
const SUPABASE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY;
const OUTPUT_DIR   = path.resolve(__dirname, '../content/drafts');

if (!SUPABASE_URL || !SUPABASE_KEY) {
  console.error('Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY');
  process.exit(1);
}

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);

function isoWeek(date) {
  const d = new Date(date);
  const jan4 = new Date(d.getFullYear(), 0, 4);
  const weekNum = Math.ceil((((d - jan4) / 86400000) + jan4.getDay() + 1) / 7);
  return `${d.getFullYear()}-W${String(weekNum).padStart(2, '0')}`;
}

function qualityBadge(quality, n) {
  const labels = { confirmed: `✅ 확인 (n=${n})`, provisional: `⚠️ 잠정 (n=${n})`, indicative: `🔵 참고 (n=${n})` };
  return labels[quality] ?? `n=${n}`;
}

function buildDelayTable(rows) {
  if (!rows || rows.length === 0) return '_이번 기간 딜레이 데이터 없음_';
  const lines = ['| 노선 | 마일스톤 | 중앙값(h) | P90(h) | 정시율 | 신뢰도 |',
                 '|------|----------|----------|--------|-------|-------|'];
  for (const r of rows) {
    const onTime = r.on_time_rate != null ? `${Math.round(r.on_time_rate * 100)}%` : '-';
    const med = r.median_delay_h != null ? `${r.median_delay_h}h` : '-';
    const p = r.p90_delay_h != null ? `${r.p90_delay_h}h` : '-';
    lines.push(`| ${r.lane_id} | ${r.milestone} | ${med} | ${p} | ${onTime} | ${qualityBadge(r.data_quality, r.sample_count)} |`);
  }
  return lines.join('\n');
}

function buildDisruptionSection(events) {
  if (!events || events.length === 0) return '이번 기간 특이 국경·정책 이슈 없음.';
  return events.map(e => `**[${e.severity.toUpperCase()}] ${e.title_ko ?? e.title_en}**\n${e.description_en ?? ''}\n${e.source_url ? `출처: ${e.source_url}` : ''}`).join('\n\n');
}

async function main() {
  const today = new Date();
  const weekIso = isoWeek(today);
  const publishDate = today.toLocaleDateString('ko-KR');

  // Fetch last 2 weeks of delay data
  const twoWeeksAgo = new Date(today.getTime() - 14 * 86400000);
  const minWeek = isoWeek(twoWeeksAgo);

  const { data: delayRows, error: delayErr } = await supabase
    .from('delay_index_weekly')
    .select('*')
    .gte('week_iso', minWeek)
    .order('week_iso', { ascending: false })
    .order('lane_id');

  if (delayErr) {
    console.error('delay_index_weekly 조회 실패:', delayErr.message);
    process.exit(1);
  }

  // Fetch active disruption events
  const { data: eventRows, error: eventErr } = await supabase
    .from('disruption_events')
    .select('*')
    .is('resolved_at', null)
    .order('severity')
    .limit(10);

  if (eventErr) {
    console.error('disruption_events 조회 실패:', eventErr.message);
    // non-fatal — continue without events
  }

  // Load template
  const templatePath = path.resolve(__dirname, '../content/templates/cadi-weekly-template.md');
  let template = fs.readFileSync(templatePath, 'utf-8');

  template = template
    .replace('{{DELAY_TABLE}}', buildDelayTable(delayRows))
    .replace('{{DISRUPTION_SECTION}}', buildDisruptionSection(eventRows ?? []))
    .replace('{{ANALYSIS_SECTION}}', '_분석 섹션: research-market-analyst 결과를 여기에 삽입_')
    .replace('{{PUBLISH_DATE}}', publishDate);

  // Write draft
  fs.mkdirSync(OUTPUT_DIR, { recursive: true });
  const filename = `cadi-report-${today.toISOString().slice(0, 10)}.md`;
  const outPath = path.join(OUTPUT_DIR, filename);
  fs.writeFileSync(outPath, template, 'utf-8');

  console.log(`✅ CADI 보고서 초안 생성: ${outPath}`);
  console.log(`  딜레이 행: ${delayRows?.length ?? 0}건, 이벤트: ${eventRows?.length ?? 0}건`);
}

main().catch(err => {
  console.error('generate-cadi-report.js 실패:', err);
  process.exit(1);
});
```

- [ ] **Step 3: `package.json`에 `report:cadi` 스크립트 추가**

```json
"report:cadi": "node scripts/generate-cadi-report.js"
```

- [ ] **Step 4: Supabase 없이 실행 시 에러 메시지 확인**

```bash
npm run report:cadi
```

Expected (env 없으면): `Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY`

(env 있으면 actual draft 생성 확인)

- [ ] **Step 5: Commit**

```bash
git add content/templates/cadi-weekly-template.md scripts/generate-cadi-report.js package.json
git commit -m "feat(report): CADI 격주 보고서 생성기 — 기존 뉴스레터 파이프라인 확장"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 4: Edge Functions (Read API)

**Files:**
- Create: `supabase/functions/cadi-lanes/index.ts`
- Create: `supabase/functions/cadi-weekly/index.ts`
- Create: `supabase/functions/alerts/index.ts`
- Create: `supabase/functions/reports/index.ts`

모든 함수: Deno, anon key(SUPABASE_ANON_KEY) 사용, `Cache-Control: public, max-age=86400` 헤더.
`shipment_legs`는 절대 노출 금지.

- [ ] **Step 1: `supabase/functions/cadi-lanes/index.ts` 작성**

```typescript
// supabase/functions/cadi-lanes/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

serve(async (_req) => {
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_ANON_KEY')!
  );

  const { data, error } = await supabase
    .from('lanes')
    .select('id, name_en, name_zh, name_ru, name_ko, transit_min, transit_max, border_points')
    .order('id');

  if (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  return new Response(JSON.stringify(data), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Cache-Control': 'public, max-age=86400',
      'Access-Control-Allow-Origin': '*',
    },
  });
});
```

- [ ] **Step 2: `supabase/functions/cadi-weekly/index.ts` 작성**

```typescript
// supabase/functions/cadi-weekly/index.ts
// Query params: ?lane_id=TCR&weeks=4
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

serve(async (req) => {
  const url = new URL(req.url);
  const laneId = url.searchParams.get('lane_id');
  const weeks = Math.min(parseInt(url.searchParams.get('weeks') ?? '4'), 12);

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_ANON_KEY')!
  );

  let query = supabase
    .from('delay_index_weekly')
    .select('lane_id, week_iso, milestone, median_delay_h, p90_delay_h, on_time_rate, sample_count, data_quality, updated_at')
    .order('week_iso', { ascending: false })
    .limit(weeks * 10); // approximate

  if (laneId) query = query.eq('lane_id', laneId);

  const { data, error } = await query;

  if (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  return new Response(JSON.stringify(data), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Cache-Control': 'public, max-age=3600', // 1시간 캐시 (daily보다 자주 갱신)
      'Access-Control-Allow-Origin': '*',
    },
  });
});
```

- [ ] **Step 3: `supabase/functions/alerts/index.ts` 작성**

```typescript
// supabase/functions/alerts/index.ts
// Returns active disruption_events (resolved_at IS NULL)
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

serve(async (req) => {
  const url = new URL(req.url);
  const laneId = url.searchParams.get('lane_id');

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_ANON_KEY')!
  );

  let query = supabase
    .from('disruption_events')
    .select('id, lane_id, event_type, title_en, title_ru, title_zh, title_ko, severity, started_at, resolved_at, source_url')
    .is('resolved_at', null)
    .order('severity')
    .limit(20);

  if (laneId) query = query.eq('lane_id', laneId);

  const { data, error } = await query;

  if (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      status: 500,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  return new Response(JSON.stringify(data), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Cache-Control': 'public, max-age=3600',
      'Access-Control-Allow-Origin': '*',
    },
  });
});
```

- [ ] **Step 4: `supabase/functions/reports/index.ts` 작성**

```typescript
// supabase/functions/reports/index.ts
// Lists published reports from content/published/ via storage bucket (future)
// For now, returns placeholder list of recent reports.
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';

serve(async (_req) => {
  // Placeholder: list will be replaced with Supabase Storage scan
  const placeholder = [
    {
      title: 'CADI Report — 준비 중',
      date: new Date().toISOString().slice(0, 10),
      url: null,
    },
  ];

  return new Response(JSON.stringify(placeholder), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Cache-Control': 'public, max-age=86400',
      'Access-Control-Allow-Origin': '*',
    },
  });
});
```

- [ ] **Step 5: Edge Functions 배포**

```bash
supabase functions deploy cadi-lanes
supabase functions deploy cadi-weekly
supabase functions deploy alerts
supabase functions deploy reports
```

Expected: 각각 `Deployed Function cadi-lanes on project ...`

- [ ] **Step 6: curl로 각 함수 검증**

```bash
# 프로젝트 URL로 대체
curl "https://<ref>.supabase.co/functions/v1/cadi-lanes" \
  -H "Authorization: Bearer <anon_key>"
# Expected: JSON array with 5 lanes

curl "https://<ref>.supabase.co/functions/v1/cadi-weekly?lane_id=TCR&weeks=4" \
  -H "Authorization: Bearer <anon_key>"
# Expected: [] (데이터 없으면), 또는 delay rows

curl "https://<ref>.supabase.co/functions/v1/alerts" \
  -H "Authorization: Bearer <anon_key>"
# Expected: [] (disruption_events 비어있으면)

# Cache-Control 헤더 확인
curl -I "https://<ref>.supabase.co/functions/v1/cadi-lanes" \
  -H "Authorization: Bearer <anon_key>"
# Expected: cache-control: public, max-age=86400
```

- [ ] **Step 7: Commit**

```bash
git add supabase/functions/
git commit -m "feat(api): CADI Edge Functions — lanes/weekly/alerts/reports (Cache-Control)"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 5: Frontend (React + Tailwind)

**Files:** 위 File Structure의 Phase 5 전체

> Phase 5는 서브태스크 5A~5E로 분리. 각 서브태스크 완료 후 `npm run build` 통과 확인.

### Task 5A: App Shell (라우팅 + i18n + API 클라이언트)

- [ ] **Step 1: `src/lib/api.ts` 작성**

```typescript
// src/lib/api.ts
// Thin wrapper around Supabase Edge Functions.
// All reads use anon client — no raw shipment_legs access.

const BASE_URL = import.meta.env.VITE_SUPABASE_URL
  ? `${import.meta.env.VITE_SUPABASE_URL}/functions/v1`
  : '';

const ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY ?? '';

async function get<T>(path: string): Promise<T> {
  const res = await fetch(`${BASE_URL}${path}`, {
    headers: { Authorization: `Bearer ${ANON_KEY}` },
  });
  if (!res.ok) throw new Error(`API ${path} failed: ${res.status}`);
  return res.json() as Promise<T>;
}

export interface Lane {
  id: string;
  name_en: string;
  name_zh: string | null;
  name_ru: string | null;
  name_ko: string | null;
  transit_min: number | null;
  transit_max: number | null;
  border_points: string[];
}

export interface DelayRow {
  lane_id: string;
  week_iso: string;
  milestone: string;
  median_delay_h: number | null;
  p90_delay_h: number | null;
  on_time_rate: number | null;
  sample_count: number;
  data_quality: 'confirmed' | 'provisional' | 'indicative';
  updated_at: string;
}

export interface DisruptionEvent {
  id: string;
  lane_id: string | null;
  event_type: string;
  title_en: string;
  title_ru: string | null;
  title_zh: string | null;
  title_ko: string | null;
  severity: 'high' | 'medium' | 'low';
  started_at: string | null;
  resolved_at: string | null;
  source_url: string | null;
}

export const api = {
  lanes: () => get<Lane[]>('/cadi-lanes'),
  weekly: (laneId?: string, weeks?: number) =>
    get<DelayRow[]>(`/cadi-weekly?${laneId ? `lane_id=${laneId}&` : ''}weeks=${weeks ?? 4}`),
  alerts: (laneId?: string) =>
    get<DisruptionEvent[]>(`/alerts${laneId ? `?lane_id=${laneId}` : ''}`),
};
```

- [ ] **Step 2: i18n 파일 작성 (5개 언어 공통 키)**

```json
// src/locales/en.json
{
  "nav": {
    "home": "Home",
    "cadi": "Delay Intelligence",
    "news": "News & Analysis",
    "methodology": "Methodology"
  },
  "home": {
    "hero": "Central Asia Delay Intelligence",
    "subtitle": "Weekly delay metrics for TCR/TSR/TITR land bridge shipments, powered by MTL data.",
    "latestAlerts": "Latest Alerts",
    "noAlerts": "No active disruptions.",
    "viewDashboard": "View CADI Dashboard"
  },
  "cadi": {
    "title": "CADI Dashboard",
    "selectLane": "Select Route",
    "medianDelay": "Median Delay (h)",
    "p90Delay": "P90 Delay (h)",
    "onTimeRate": "On-Time Rate",
    "sampleCount": "Sample (n)",
    "noData": "No delay data for the selected period.",
    "quality": {
      "confirmed": "Confirmed",
      "provisional": "Provisional",
      "indicative": "Indicative"
    }
  },
  "methodology": {
    "title": "Methodology",
    "trustNote": "Based on MTL-handled shipments only. Not representative of the full market."
  },
  "loading": "Loading...",
  "error": "Failed to load data."
}
```

```json
// src/locales/ko.json
{
  "nav": {
    "home": "홈",
    "cadi": "딜레이 인텔리전스",
    "news": "뉴스 & 분석",
    "methodology": "방법론"
  },
  "home": {
    "hero": "중앙아시아 딜레이 인텔리전스",
    "subtitle": "TCR/TSR/TITR 랜드브리지 화물 딜레이 주간 집계 — MTL 처리 화물 기준.",
    "latestAlerts": "최신 알림",
    "noAlerts": "현재 활성 이슈 없음.",
    "viewDashboard": "CADI 대시보드 보기"
  },
  "cadi": {
    "title": "CADI 대시보드",
    "selectLane": "노선 선택",
    "medianDelay": "중앙값 딜레이 (h)",
    "p90Delay": "P90 딜레이 (h)",
    "onTimeRate": "정시율",
    "sampleCount": "표본 (n)",
    "noData": "선택 기간 딜레이 데이터 없음.",
    "quality": {
      "confirmed": "확인",
      "provisional": "잠정",
      "indicative": "참고"
    }
  },
  "methodology": {
    "title": "방법론",
    "trustNote": "MTL 처리 화물만을 대상으로 집계한 값입니다. 시장 전체를 대표하지 않습니다."
  },
  "loading": "로딩 중...",
  "error": "데이터 로드 실패."
}
```

```json
// src/locales/ru.json
{
  "nav": {
    "home": "Главная",
    "cadi": "Аналитика задержек",
    "news": "Новости и анализ",
    "methodology": "Методология"
  },
  "home": {
    "hero": "Аналитика задержек в Центральной Азии",
    "subtitle": "Еженедельные метрики задержек для маршрутов TCR/TSR/TITR.",
    "latestAlerts": "Последние уведомления",
    "noAlerts": "Нет активных нарушений.",
    "viewDashboard": "Перейти в CADI"
  },
  "cadi": {
    "title": "Панель CADI",
    "selectLane": "Выбор маршрута",
    "medianDelay": "Медианная задержка (ч)",
    "p90Delay": "P90 задержка (ч)",
    "onTimeRate": "Пунктуальность",
    "sampleCount": "Выборка (n)",
    "noData": "Нет данных за выбранный период.",
    "quality": {
      "confirmed": "Подтверждено",
      "provisional": "Предварительно",
      "indicative": "Ориентировочно"
    }
  },
  "methodology": {
    "title": "Методология",
    "trustNote": "Данные основаны только на грузах, обработанных MTL."
  },
  "loading": "Загрузка...",
  "error": "Ошибка загрузки данных."
}
```

```json
// src/locales/uz.json
{
  "nav": {
    "home": "Bosh sahifa",
    "cadi": "Kechikish tahlili",
    "news": "Yangiliklar va tahlil",
    "methodology": "Metodologiya"
  },
  "home": {
    "hero": "Markaziy Osiyo kechikish tahlili",
    "subtitle": "TCR/TSR/TITR yo'nalishlari uchun haftalik kechikish ko'rsatkichlari.",
    "latestAlerts": "So'nggi ogohlantirishlar",
    "noAlerts": "Faol buzilishlar yo'q.",
    "viewDashboard": "CADI panelini ko'rish"
  },
  "cadi": {
    "title": "CADI paneli",
    "selectLane": "Yo'nalishni tanlang",
    "medianDelay": "O'rtacha kechikish (soat)",
    "p90Delay": "P90 kechikish (soat)",
    "onTimeRate": "O'z vaqtida yetkazish",
    "sampleCount": "Namuna (n)",
    "noData": "Tanlangan davr uchun ma'lumot yo'q.",
    "quality": {
      "confirmed": "Tasdiqlangan",
      "provisional": "Vaqtinchalik",
      "indicative": "Ko'rsatma"
    }
  },
  "methodology": {
    "title": "Metodologiya",
    "trustNote": "Ma'lumotlar faqat MTL tomonidan qayta ishlangan yuklarga asoslanadi."
  },
  "loading": "Yuklanmoqda...",
  "error": "Ma'lumotlarni yuklashda xato."
}
```

```json
// src/locales/zh.json
{
  "nav": {
    "home": "首页",
    "cadi": "延误智能",
    "news": "新闻与分析",
    "methodology": "方法论"
  },
  "home": {
    "hero": "中亚延误智能 (CADI)",
    "subtitle": "TCR/TSR/TITR陆桥货物每周延误指标，基于MTL处理数据。",
    "latestAlerts": "最新预警",
    "noAlerts": "当前无活跃干扰事件。",
    "viewDashboard": "查看CADI仪表板"
  },
  "cadi": {
    "title": "CADI仪表板",
    "selectLane": "选择线路",
    "medianDelay": "中位延误(小时)",
    "p90Delay": "P90延误(小时)",
    "onTimeRate": "准时率",
    "sampleCount": "样本(n)",
    "noData": "所选时段无延误数据。",
    "quality": {
      "confirmed": "已确认",
      "provisional": "暂定",
      "indicative": "参考"
    }
  },
  "methodology": {
    "title": "方法论",
    "trustNote": "数据仅基于MTL处理的货物，不代表整体市场。"
  },
  "loading": "加载中...",
  "error": "数据加载失败。"
}
```

- [ ] **Step 3: `src/App.tsx` 작성**

```tsx
// src/App.tsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';
import { Suspense } from 'react';
import { useTranslation } from 'react-i18next';
import Home from './pages/Home';
import CadiDashboard from './pages/CadiDashboard';
import NewsAnalysis from './pages/NewsAnalysis';
import Methodology from './pages/Methodology';
import './i18n';

function Nav() {
  const { t, i18n } = useTranslation();
  return (
    <nav className="bg-slate-900 text-white px-6 py-3 flex items-center gap-6">
      <span className="font-bold text-lg tracking-tight">Logisight</span>
      <Link to="/" className="hover:text-sky-400 transition-colors">{t('nav.home')}</Link>
      <Link to="/cadi" className="hover:text-sky-400 transition-colors">{t('nav.cadi')}</Link>
      <Link to="/news" className="hover:text-sky-400 transition-colors">{t('nav.news')}</Link>
      <Link to="/methodology" className="hover:text-sky-400 transition-colors">{t('nav.methodology')}</Link>
      <div className="ml-auto flex gap-2 text-sm">
        {['ko', 'en', 'ru', 'uz', 'zh'].map(lng => (
          <button
            key={lng}
            onClick={() => i18n.changeLanguage(lng)}
            className={`px-2 py-0.5 rounded ${i18n.language === lng ? 'bg-sky-600' : 'hover:bg-slate-700'}`}
          >
            {lng.toUpperCase()}
          </button>
        ))}
      </div>
    </nav>
  );
}

export default function App() {
  return (
    <BrowserRouter>
      <div className="min-h-screen bg-slate-50">
        <Nav />
        <Suspense fallback={<div className="p-8 text-center text-slate-500">Loading...</div>}>
          <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/cadi" element={<CadiDashboard />} />
            <Route path="/news" element={<NewsAnalysis />} />
            <Route path="/methodology" element={<Methodology />} />
          </Routes>
        </Suspense>
      </div>
    </BrowserRouter>
  );
}
```

- [ ] **Step 4: `src/i18n.ts` 작성**

```typescript
// src/i18n.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import en from './locales/en.json';
import ko from './locales/ko.json';
import ru from './locales/ru.json';
import uz from './locales/uz.json';
import zh from './locales/zh.json';

i18n.use(initReactI18next).init({
  resources: { en: { translation: en }, ko: { translation: ko }, ru: { translation: ru }, uz: { translation: uz }, zh: { translation: zh } },
  lng: 'ko',
  fallbackLng: 'en',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 5: 빌드 통과 확인**

```bash
npm run build
```

Expected: `dist/` 생성, 에러 없음

(타입 에러 있으면 해결 후 재빌드)

- [ ] **Step 6: Commit (5A)**

```bash
git add src/
git commit -m "feat(frontend/5A): App shell — i18n, routing, API client"
git push
```

### Task 5B: 공유 컴포넌트

- [ ] **Step 1: `src/components/shared/LoadingState.tsx`**

```tsx
export default function LoadingState({ text }: { text?: string }) {
  return (
    <div className="flex flex-col items-center justify-center py-16 text-slate-400">
      <div className="w-8 h-8 border-2 border-sky-500 border-t-transparent rounded-full animate-spin mb-3" />
      <p>{text ?? 'Loading...'}</p>
    </div>
  );
}
```

- [ ] **Step 2: `src/components/shared/EmptyState.tsx`**

```tsx
export default function EmptyState({ message }: { message: string }) {
  return (
    <div className="flex items-center justify-center py-16 text-slate-400 text-sm">
      {message}
    </div>
  );
}
```

- [ ] **Step 3: `src/components/cadi/DataQualityBadge.tsx`**

```tsx
import { useTranslation } from 'react-i18next';

const COLORS = {
  confirmed:   'bg-emerald-100 text-emerald-800',
  provisional: 'bg-amber-100  text-amber-800',
  indicative:  'bg-sky-100    text-sky-800',
} as const;

export default function DataQualityBadge({
  quality,
  sampleCount,
}: {
  quality: 'confirmed' | 'provisional' | 'indicative';
  sampleCount: number;
}) {
  const { t } = useTranslation();
  return (
    <span className={`inline-flex items-center gap-1 px-2 py-0.5 rounded text-xs font-medium ${COLORS[quality]}`}>
      {t(`cadi.quality.${quality}`)} (n={sampleCount})
    </span>
  );
}
```

- [ ] **Step 4: `src/components/cadi/LaneSelector.tsx`**

```tsx
import type { Lane } from '../../lib/api';
import { useTranslation } from 'react-i18next';

export default function LaneSelector({
  lanes,
  selected,
  onChange,
}: {
  lanes: Lane[];
  selected: string;
  onChange: (id: string) => void;
}) {
  const { t } = useTranslation();
  return (
    <div className="flex gap-2 flex-wrap">
      <span className="text-sm text-slate-500 self-center">{t('cadi.selectLane')}:</span>
      {lanes.map(lane => (
        <button
          key={lane.id}
          onClick={() => onChange(lane.id)}
          className={`px-3 py-1 rounded-full text-sm border transition-colors ${
            selected === lane.id
              ? 'bg-slate-900 text-white border-slate-900'
              : 'border-slate-300 hover:border-slate-500'
          }`}
        >
          {lane.id}
          <span className="ml-1 text-xs text-slate-400">
            {lane.transit_min}–{lane.transit_max}d
          </span>
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 5: `src/components/cadi/AlertCard.tsx`**

```tsx
import type { DisruptionEvent } from '../../lib/api';
import { useTranslation } from 'react-i18next';

const SEVERITY_STYLE = {
  high:   'border-red-400 bg-red-50',
  medium: 'border-amber-400 bg-amber-50',
  low:    'border-sky-400 bg-sky-50',
} as const;

export default function AlertCard({ event }: { event: DisruptionEvent }) {
  const { i18n } = useTranslation();
  const lng = i18n.language as 'ko' | 'en' | 'ru' | 'zh';
  const title = (lng === 'ko' && event.title_ko) || (lng === 'ru' && event.title_ru) ||
                (lng === 'zh' && event.title_zh) || event.title_en;
  return (
    <div className={`rounded-lg border-l-4 p-4 ${SEVERITY_STYLE[event.severity]}`}>
      <div className="flex items-center gap-2 mb-1">
        <span className="text-xs font-semibold uppercase tracking-wide text-slate-500">
          {event.event_type.replace('_', ' ')}
        </span>
        {event.lane_id && (
          <span className="text-xs bg-slate-200 px-2 rounded">{event.lane_id}</span>
        )}
      </div>
      <p className="font-medium text-slate-800">{title}</p>
      {event.source_url && (
        <a href={event.source_url} target="_blank" rel="noopener noreferrer"
           className="text-xs text-sky-600 hover:underline mt-1 block">
          원문 보기 →
        </a>
      )}
    </div>
  );
}
```

- [ ] **Step 6: `src/components/cadi/DelayChart.tsx`**

```tsx
import {
  LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer,
} from 'recharts';
import type { DelayRow } from '../../lib/api';
import { useTranslation } from 'react-i18next';

interface Props {
  rows: DelayRow[];
  milestone: string;
}

export default function DelayChart({ rows, milestone }: Props) {
  const { t } = useTranslation();

  const filtered = rows
    .filter(r => r.milestone === milestone)
    .sort((a, b) => a.week_iso.localeCompare(b.week_iso));

  if (filtered.length === 0) {
    return <p className="text-slate-400 text-sm py-4">{t('cadi.noData')}</p>;
  }

  const data = filtered.map(r => ({
    week: r.week_iso,
    [t('cadi.medianDelay')]: r.median_delay_h,
    [t('cadi.p90Delay')]: r.p90_delay_h,
  }));

  return (
    <ResponsiveContainer width="100%" height={240}>
      <LineChart data={data} margin={{ top: 8, right: 8, left: 0, bottom: 0 }}>
        <CartesianGrid strokeDasharray="3 3" stroke="#e2e8f0" />
        <XAxis dataKey="week" tick={{ fontSize: 11 }} />
        <YAxis tick={{ fontSize: 11 }} unit="h" />
        <Tooltip />
        <Legend />
        <Line type="monotone" dataKey={t('cadi.medianDelay')} stroke="#0ea5e9" strokeWidth={2} dot={false} />
        <Line type="monotone" dataKey={t('cadi.p90Delay')} stroke="#f59e0b" strokeWidth={2} strokeDasharray="5 5" dot={false} />
      </LineChart>
    </ResponsiveContainer>
  );
}
```

- [ ] **Step 7: 빌드 통과 확인**

```bash
npm run build
```

- [ ] **Step 8: Commit (5B)**

```bash
git add src/components/
git commit -m "feat(frontend/5B): shared + CADI components — DataQualityBadge, DelayChart, AlertCard, LaneSelector"
git push
```

### Task 5C: 페이지 구현

- [ ] **Step 1: `src/pages/Home.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { useTranslation } from 'react-i18next';
import { api, type DisruptionEvent, type DelayRow } from '../lib/api';
import AlertCard from '../components/cadi/AlertCard';
import DataQualityBadge from '../components/cadi/DataQualityBadge';
import LoadingState from '../components/shared/LoadingState';
import EmptyState from '../components/shared/EmptyState';

export default function Home() {
  const { t } = useTranslation();
  const [alerts, setAlerts] = useState<DisruptionEvent[]>([]);
  const [snapshot, setSnapshot] = useState<DelayRow[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    Promise.all([api.alerts(), api.weekly(undefined, 1)])
      .then(([a, w]) => { setAlerts(a); setSnapshot(w); })
      .catch(e => setError(e.message))
      .finally(() => setLoading(false));
  }, []);

  if (loading) return <LoadingState text={t('loading')} />;

  return (
    <main className="max-w-4xl mx-auto px-6 py-12">
      <h1 className="text-3xl font-bold text-slate-900 mb-2">{t('home.hero')}</h1>
      <p className="text-slate-500 mb-8">{t('home.subtitle')}</p>

      {error && <p className="text-red-500 mb-4">{t('error')}: {error}</p>}

      {/* Snapshot */}
      {snapshot.length > 0 && (
        <section className="mb-8">
          <h2 className="text-lg font-semibold text-slate-700 mb-3">
            이번 주 딜레이 스냅샷
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
            {snapshot.slice(0, 4).map(row => (
              <div key={`${row.lane_id}-${row.milestone}`} className="bg-white rounded-lg p-4 border border-slate-200">
                <div className="flex items-center justify-between mb-1">
                  <span className="font-medium text-slate-800">{row.lane_id} — {row.milestone}</span>
                  <DataQualityBadge quality={row.data_quality} sampleCount={row.sample_count} />
                </div>
                <p className="text-2xl font-bold text-slate-900">
                  {row.median_delay_h != null ? `${row.median_delay_h}h` : '—'}
                </p>
                <p className="text-xs text-slate-400">중앙값 딜레이 | P90: {row.p90_delay_h ?? '—'}h</p>
              </div>
            ))}
          </div>
          <Link to="/cadi" className="mt-3 inline-block text-sky-600 hover:underline text-sm">
            {t('home.viewDashboard')} →
          </Link>
        </section>
      )}

      {/* Alerts */}
      <section>
        <h2 className="text-lg font-semibold text-slate-700 mb-3">{t('home.latestAlerts')}</h2>
        {alerts.length === 0
          ? <EmptyState message={t('home.noAlerts')} />
          : <div className="flex flex-col gap-3">{alerts.slice(0, 3).map(a => <AlertCard key={a.id} event={a} />)}</div>
        }
      </section>
    </main>
  );
}
```

- [ ] **Step 2: `src/pages/CadiDashboard.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { api, type Lane, type DelayRow, type DisruptionEvent } from '../lib/api';
import LaneSelector from '../components/cadi/LaneSelector';
import DelayChart from '../components/cadi/DelayChart';
import DataQualityBadge from '../components/cadi/DataQualityBadge';
import AlertCard from '../components/cadi/AlertCard';
import LoadingState from '../components/shared/LoadingState';
import EmptyState from '../components/shared/EmptyState';

const MILESTONES = [
  'ORIGIN_DEP', 'RAIL_DEP_CN', 'KASHI_ARR', 'CN_BORDER', 'KG_UZ_BORDER', 'DEST_ARR'
] as const;

export default function CadiDashboard() {
  const { t } = useTranslation();
  const [lanes, setLanes] = useState<Lane[]>([]);
  const [selectedLane, setSelectedLane] = useState('TCR');
  const [rows, setRows] = useState<DelayRow[]>([]);
  const [alerts, setAlerts] = useState<DisruptionEvent[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    api.lanes()
      .then(l => { setLanes(l); if (l.length > 0) setSelectedLane(l[0].id); })
      .catch(e => setError(e.message));
  }, []);

  useEffect(() => {
    setLoading(true);
    Promise.all([api.weekly(selectedLane, 8), api.alerts(selectedLane)])
      .then(([w, a]) => { setRows(w); setAlerts(a); })
      .catch(e => setError(e.message))
      .finally(() => setLoading(false));
  }, [selectedLane]);

  return (
    <main className="max-w-5xl mx-auto px-6 py-10">
      <h1 className="text-2xl font-bold text-slate-900 mb-6">{t('cadi.title')}</h1>

      {error && <p className="text-red-500 mb-4">{t('error')}: {error}</p>}

      <LaneSelector lanes={lanes} selected={selectedLane} onChange={setSelectedLane} />

      {loading ? (
        <LoadingState text={t('loading')} />
      ) : rows.length === 0 ? (
        <EmptyState message={t('cadi.noData')} />
      ) : (
        <>
          {/* Charts per key milestone */}
          <div className="mt-8 grid grid-cols-1 md:grid-cols-2 gap-8">
            {MILESTONES.map(ms => (
              <div key={ms} className="bg-white rounded-lg border border-slate-200 p-4">
                <h3 className="text-sm font-semibold text-slate-600 mb-3">{ms}</h3>
                <DelayChart rows={rows} milestone={ms} />
                {/* Show latest sample badge */}
                {rows.filter(r => r.milestone === ms)[0] && (
                  <div className="mt-2">
                    <DataQualityBadge
                      quality={rows.filter(r => r.milestone === ms)[0].data_quality}
                      sampleCount={rows.filter(r => r.milestone === ms)[0].sample_count}
                    />
                  </div>
                )}
              </div>
            ))}
          </div>

          {/* Disruption events */}
          {alerts.length > 0 && (
            <section className="mt-10">
              <h2 className="text-lg font-semibold text-slate-700 mb-3">Disruption Alerts</h2>
              <div className="flex flex-col gap-3">
                {alerts.map(a => <AlertCard key={a.id} event={a} />)}
              </div>
            </section>
          )}
        </>
      )}
    </main>
  );
}
```

- [ ] **Step 3: `src/pages/NewsAnalysis.tsx`**

```tsx
import { useTranslation } from 'react-i18next';

export default function NewsAnalysis() {
  const { t } = useTranslation();
  return (
    <main className="max-w-4xl mx-auto px-6 py-12">
      <h1 className="text-2xl font-bold text-slate-900 mb-4">{t('nav.news')}</h1>
      <p className="text-slate-500">
        뉴스 피드 — 자동 수집된 뉴스가 여기에 표시됩니다.
        (workers/collectors 파이프라인 연동 후 활성화)
      </p>
    </main>
  );
}
```

- [ ] **Step 4: `src/pages/Methodology.tsx`**

```tsx
import { useTranslation } from 'react-i18next';

export default function Methodology() {
  const { t } = useTranslation();
  return (
    <main className="max-w-3xl mx-auto px-6 py-12">
      <h1 className="text-2xl font-bold text-slate-900 mb-6">{t('methodology.title')}</h1>

      <section className="prose prose-slate max-w-none">
        <h2>데이터 출처</h2>
        <p>
          CADI는 MTL Shipping Agency가 직접 처리한 화물의 마일스톤 타임스탬프를
          집계한 지수입니다. 외부 공개 데이터가 아닌 MTL 내부 운영 데이터 기반입니다.
        </p>

        <h2>마일스톤 정의</h2>
        <ul>
          <li><strong>ORIGIN_DEP</strong>: 한국 출발항 또는 공장 출고</li>
          <li><strong>SEA_TS_ARR</strong>: 해상 환적항 도착</li>
          <li><strong>RAIL_DEP_CN</strong>: 중국 철도 출발 (CR Express 탑재)</li>
          <li><strong>KASHI_ARR</strong>: 카스시 / 우루무치 도착</li>
          <li><strong>KASHI_BONDED</strong>: 카스시 보세창고 통관</li>
          <li><strong>TRUCK_DEP</strong>: 트럭 환적 출발</li>
          <li><strong>XIAN_HUB</strong>: 시안 허브 처리</li>
          <li><strong>CN_BORDER</strong>: 중국 국경 출경 (Horgos/Dostyk/Erenhot)</li>
          <li><strong>KG_UZ_BORDER</strong>: 중앙아시아 국경 입경</li>
          <li><strong>DEST_ARR</strong>: 최종 목적지 도착</li>
        </ul>

        <h2>집계 방법</h2>
        <ul>
          <li>딜레이 = 실제 도착 − 계획 도착 (시간 단위, 음수 = 조기)</li>
          <li>중앙값(Median): 주간 표본의 50th percentile</li>
          <li>P90: 주간 표본의 90th percentile (최악 케이스 대리지표)</li>
          <li>정시율: 딜레이 ≤ 0인 비율</li>
        </ul>

        <h2>데이터 신뢰도 등급</h2>
        <ul>
          <li>✅ <strong>Confirmed</strong>: n ≥ 5</li>
          <li>⚠️ <strong>Provisional</strong>: 2 ≤ n &lt; 5</li>
          <li>🔵 <strong>Indicative</strong>: n &lt; 2</li>
        </ul>

        <p className="text-sm text-slate-500 mt-8">
          {t('methodology.trustNote')}
        </p>
      </section>
    </main>
  );
}
```

- [ ] **Step 5: `npm run dev` 로 로컬 확인**

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속 후:
- [ ] 홈 페이지 렌더링 확인
- [ ] `/cadi` 라우팅 확인
- [ ] `/methodology` 확인
- [ ] 언어 전환 (ko/en/ru) 확인

- [ ] **Step 6: 빌드 통과**

```bash
npm run build
```

Expected: 에러 없음

- [ ] **Step 7: Commit (5C)**

```bash
git add src/pages/ src/App.tsx src/i18n.ts src/locales/
git commit -m "feat(frontend/5C): pages — Home, CadiDashboard, NewsAnalysis, Methodology"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## Task 6: Automation (GitHub Actions 확장)

**Files:**
- Create: `.github/workflows/cadi-ingest.yml`
- Modify: `.github/workflows/weekly-report.yml` (CADI 섹션 추가)

- [ ] **Step 1: `.github/workflows/cadi-ingest.yml` 작성**

```yaml
# .github/workflows/cadi-ingest.yml
# CADI xlsx ingest — 수동 트리거 전용 (자동 실행은 xlsx 파일 준비 후 cron 추가)
name: CADI Tracing Ingest

on:
  workflow_dispatch:   # 수동 실행
    inputs:
      dry_run:
        description: 'Dry run (no DB write)'
        required: false
        default: 'false'

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run CADI tracing ingest
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: npm run collect:tracing
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Dry run check
        if: ${{ github.event.inputs.dry_run == 'true' }}
        run: echo "DRY RUN — skipping DB write"
```

- [ ] **Step 2: `.github/workflows/weekly-report.yml` 수정 — CADI 단계 추가**

기존 `generate-report` step 뒤에 다음 step 삽입:

```yaml
      - name: Generate CADI report
        run: npm run report:cadi
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
```

- [ ] **Step 3: GitHub Secrets 확인**

Repository → Settings → Secrets and variables → Actions:
- `SUPABASE_URL` 추가
- `SUPABASE_SERVICE_ROLE_KEY` 추가
- 기존 `ANTHROPIC_API_KEY`, `RESEND_API_KEY` 유지

- [ ] **Step 4: `workflow_dispatch`로 dry-run 테스트**

GitHub Actions → CADI Tracing Ingest → Run workflow → dry_run: true

Expected: `DRY RUN — skipping DB write`

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/cadi-ingest.yml .github/workflows/weekly-report.yml
git commit -m "feat(automation): CADI GitHub Actions — ingest workflow + weekly-report CADI step"
git push
```

→ `dev-code-reviewer` 호출 권장

---

## 완료 체크리스트

- [ ] CLAUDE.md 규칙(명명·RLS·의존성 승인·외부데이터) 준수
- [ ] 기존 collectors·scripts·content·workflows 무손상
- [ ] Supabase 클라우드 연결 + 마이그레이션 적용
- [ ] RLS: anon은 shipment_legs 조회 불가 (SELECT count() = 0 확인)
- [ ] collect:tracing — 멱등, 플래그 로그 출력
- [ ] 주보: 빈 섹션 자동 생략({{DELAY_TABLE}} 없으면 '_데이터 없음_' 표시)
- [ ] CADI 대시보드 — DataQualityBadge 표시, empty state 처리
- [ ] Methodology 페이지 공개 (신뢰 표기 포함)
- [ ] `npm run build` 통과 (타입 에러 0)
- [ ] `npm run dev` 로컬 동작 확인
- [ ] 전 Phase 커밋·푸시·dev-code-reviewer 완료

---

## ⚠️ 스펙 파일 수령 시 업데이트 항목

| 파일 | 업데이트 항목 |
|------|------------|
| `workers/collectors/utils/xlsx_parser.ts` | `MILESTONE_KEYWORDS` 배열 — 실제 컬럼명 추가 |
| `content/templates/cadi-weekly-template.md` | 섹션 구조를 CentralAsia_Weekly_Template 기준으로 교체 |
| `src/pages/CadiDashboard.tsx` | `MILESTONES` 배열 — Content_Site_Architecture IA 반영 |
| `supabase/migrations/` | CADI_Spec_v1.1 10절 추가 컬럼·제약 있으면 새 마이그레이션 추가 |
