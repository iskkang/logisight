-- generated_by 는 모델명이 아니라 바이라인 표기값이다.
--
-- 20260814000001 은 이 열에 모델 이름을 넣기로 하고 그렇게 설명을 달았다
-- ("예: deepseek-v4-flash"). 그 결정을 뒤집는다.
--
-- 독자에게 필요한 정보는 "누가 책임지는가"이지 "어느 모델을 썼는가"가 아니다.
-- 모델명을 넣으면 모델을 교체할 때마다 지난 기사들의 표기가 제각각이 되고,
-- 화면에는 어차피 "Logisight AI 정리"로 나가므로 저장값과 표시값이 어긋난다.
-- 어느 모델이 실제로 돌았는지는 실행 로그가 남긴다.
--
-- 적용된 마이그레이션 파일 자체는 고치지 않는다 —— 이미 돌아간 것을 나중에
-- 고쳐 쓰면 파일과 실제 DB 가 어긋난다. 여기서 덮어쓴다.
--
-- 소급 정리는 필요 없다: 2026-08-15 실측으로 generated_by 가 비어 있지 않은 행은
-- 0건이었다(모델명이 들어간 기사가 아직 없다).

comment on column public.maritime_news.generated_by is
  '우리 AI 가 원문을 정리한 기사면 Logisight AI. 사람이 쓴 기사와 표기 시작 이전 기사는 NULL. 모델명을 넣지 않는다.';
