name: Monthly Freight Rates
# DISABLED: logisight-web 제거로 scripts/fetch-freight-rates.ts 삭제됨.
# 재구현 필요: 관세청 API 호출 로직을 collectors/ 로 이전 후 활성화.

on:
  workflow_dispatch:   # 수동 트리거만 허용 (schedule 비활성화)

jobs:
  fetch-freight-rates:
    if: false   # disabled until re-implemented in collectors/
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Fetch freight rates (KR POL)
        run: echo "TODO - re-implement fetch-freight-rates in collectors/"
        env:
          DATA_GO_KR_API_KEY:        ${{ secrets.DATA_GO_KR_API_KEY }}
          SUPABASE_URL:              ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
